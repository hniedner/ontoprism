# Evidence admission

This directory admits the exact group-review Markdown, its compact JSON sidecar, and the
byte-identical historical schema-3 packet that binds them. The Markdown is the authoritative
contextual human evidence. The sidecar is only a digest and operational binding. The historical
packet is retained for interpretation and is not converted into the active schema-4 review model.

Do not admit XLSX, TTL, NT, OWL, logs, dumps, corpora, licensed material, or HEAD-bound output.
Rationale claims remain human interpretation unless separately corroborated. A local-SME review
outcome is not NCI acceptance, publication, or publication authorization.
