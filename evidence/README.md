# Evidence admission

This directory's first admission contains only the exact group-review Markdown and its compact
JSON sidecar. The Markdown is the authoritative contextual human evidence. The sidecar is only a
digest and operational binding; machine context derives from the strictly bound packet.

Do not admit XLSX, TTL, NT, OWL, logs, dumps, corpora, licensed material, or HEAD-bound output.
Rationale claims remain human interpretation unless separately corroborated. A local-SME review
outcome is not NCI acceptance, publication, or publication authorization.
