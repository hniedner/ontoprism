# Tooling tests

This root contains tests for repository automation, validation, agent wrappers, and
test infrastructure rather than backend product behaviour. `pdm run test-unit`,
`pdm run verify`, and every CI backend and integration partition include this root.
