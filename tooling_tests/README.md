# Tooling tests

This root contains tests for repository automation, validation, agent wrappers, and
test infrastructure rather than backend product behaviour. Local verification runs it
only when tooling inputs changed; CI still runs it whenever its path filter selects the
tooling lane.
