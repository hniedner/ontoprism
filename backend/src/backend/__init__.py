"""backend — FastAPI application for the ontoprism ontology platform.

Lifted from fairdata's backend, scoped to the repository/graph/search/sparql/refresh
surface. Frontend talks only to this backend, which owns all Oxigraph/Postgres access.
"""

__version__ = "0.1.0"
