[project]
name = "ontoprism-backend"
version = "0.8.1"
description = "FastAPI backend for ontoprism (imported as `backend`)."
requires-python = ">=3.13,<3.14"
dependencies = ["ontolib"]

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"

[tool.pdm.build]
package-dir = "src"
