"""ontolib — ontology/terminology core, lifted from the fairdata project.

Only the ontology vertical slice is carried over (storage, terminologies, cadsr,
core, common). The fairdata pipeline/HRM/learning/audit subsystems are intentionally
not part of ontoprism.
"""

__version__ = "0.13.0"
